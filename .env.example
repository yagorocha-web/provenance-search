GEMINI_API_KEY=
TAVILY_API_KEY=
EUROPEANA_API_KEY=
PORT=3000

# Comma-separated CORS allowlist for browser callers. Same-origin requests from
# the bundled frontend are always allowed, so this is only needed when another
# site must call this API. Default: http://localhost:3000,http://127.0.0.1:3000
ALLOWED_ORIGINS=
